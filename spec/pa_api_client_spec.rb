require 'spec_helper'

describe PaApiClient do
  it 'has a version number' do
    expect(PaApiClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
